name: client
description: "Http client for network requests."
version: 0.0.1
homepage: "none"
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: any
  path: any
  meta: any
  http_parser: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
