name: analytics
description: "Package for analytics and tracking features in the application."
version: 0.0.1
homepage:

resolution: workspace

environment:
  sdk: ^3.11.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  collection: any
  meta: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
